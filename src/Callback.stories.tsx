import React from 'react';
import { render } from 'react-dom';
import DiscordCallback from './DiscordCallback';
import DiscordLogin from './DiscordLogin';

export const Callback = () => <DiscordCallback/>

export default {
  title: 'Discord Login/Callback',
  component: Callback,
}
